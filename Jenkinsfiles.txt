pipeline {

  agent any
  stages {
   

    stage('Build') {
    
      steps {                
      git 'https://github.com/Indhuja-R/php.git'
      
       withMaven(maven : 'apache-maven-3.6.1) {
       
      sh "mvn clean verify"
    } 

      }
    }
    stage('test') {
    
      steps {                
          sh "mvn clean verify"

      }
    }
    
  }
}
